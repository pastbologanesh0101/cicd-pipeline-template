"""Sample Flask application used as the subject of the CI/CD pipeline.

This package intentionally contains small, real business logic
(a calculator module and an in-memory todo store) so that the
lint/test/security stages in the pipeline have something genuine
to check.
"""

__version__ = "1.0.0"
